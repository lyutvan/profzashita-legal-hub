type LandingConsultationFormProps = {
  onSuccess?: () => void;
  submitLabel?: string;
};

const LandingConsultationForm = (_props: LandingConsultationFormProps) => null;

export default LandingConsultationForm;
